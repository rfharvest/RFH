@model RFH.Models.HomeStatisticsViewModel

<ul id="ticker">
  @foreach (var item in Model.StatisticsItems) {
    <li class="tickerItem"><span class="tickerDescription">@item.Description</span>: @item.Value.ToString("N0") @item.Units</li>
  }
</ul>