@model RFH.Models.HomeNewsFeedViewModel

<ul id="newsfeedticker" class="ticker">
    @foreach (var item in Model.NewsFeedItems)
    {
        <li class="ticker"><span class="tickerDescription">@item.Description</span></li>
    }
</ul>
