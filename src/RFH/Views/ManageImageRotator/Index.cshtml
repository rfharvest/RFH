@using RFH.Models
@model ManageImageIndexViewModel

<h2>@Html.Raw(@PageData["SectionTitle"]) List</h2>

<div id="manageImageIndex">

    <p>@Html.ActionLink("Upload Image", "Upload")</p>

    <div class="formContainer">

        <ul>
            @foreach (var imageUrl in Model.ImageUrls)
            {
                <li><a href="/ManageImageRotator/Details?fileName=@imageUrl" title="@imageUrl"><img src="@Model.FolderUrl/@imageUrl" alt="" /></a></li>
            }
        </ul>

    </div>

    <p>@Html.ActionLink("Upload Image", "Upload")</p>

</div>